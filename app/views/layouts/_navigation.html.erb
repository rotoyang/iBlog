<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
  <div class="container-fluid">
    <%= link_to "iBlog", root_path, class: "navbar-brand", id: "logo" %>>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <ul class="navbar-nav me-auto mb-2 mb-lg-0">
        <li class="nav-item">
          <a class="nav-link" href="/about">About</a>
        </li>
        <li class="nav-item dropdown">
          <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-bs-toggle="dropdown" aria-expanded="false">
            Posts
          </a>
          <ul class="dropdown-menu" aria-labelledby="navbarDropdown">
            <li><%= link_to 'New Post', new_post_path, class: "dropdown-item" %></li>
            <li><%= link_to "View Posts", posts_path, class: "dropdown-item" %></li>
            <li><hr class="dropdown-divider"></li>
            <% if current_user %>
              <li><%= link_to('Logout', destroy_user_session_path, :method => :delete, class: "dropdown-item") %></li>
              <li><%= link_to('Edit User', edit_registration_path(:user), class: "dropdown-item") %></li>
            <% else %>
              <li><%= link_to('Sign up', new_registration_path(:user), class: "dropdown-item") %></li>
              <li><%= link_to('Log in', new_session_path(:user), class: "dropdown-item") %></li>
            <% end %>
          </ul>
        </li>
      </ul>
    </div>
  </div>
</nav>